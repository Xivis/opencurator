

let INITIAL_STATE = {
  data: {
    '0x1234': {
      loading: false
    }
  }
}

export function tokenReducer(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      break;
  }

  return state;
}
